services:
  - type: web
    name: luna-foodhub
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: GOOGLE_PLACES_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000
